# BACKLOG.md — งานที่เกินขอบเขต Module 2 เก็บไว้ทำ Sprint ถัดไป

> ไฟล์นี้เก็บข้อควรระวัง/งานที่เกินขอบเขตสัปดาห์ 6–9 ตาม `leaveeasy-spec.md` §0.1 ("ห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า") — บันทึกไว้แทนที่จะลงมือทำตอนนี้

## 🔑 Sprint 2 — การจัดการคีย์ AI (OpenRouter) ให้ปลอดภัยจริง

เกิดจากการทำ `test-ai.html` (หน้าทดสอบเรียก OpenRouter) และเตรียมไว้ก่อนทำ US-09 (ปุ่มผู้ช่วย AI จริง สัปดาห์ที่ 8):

- [ ] **ห้ามใช้คีย์ส่วนตัวของนักศึกษาในโค้ดที่จะ deploy จริงเด็ดขาด** ใช้เฉพาะคีย์ที่หลักสูตร/ผู้สอนออกให้และตั้งวงเงินการใช้งาน (budget/rate limit) ไว้แล้วเท่านั้น
  เหตุผล: คีย์ส่วนตัวไม่มีเพดานการใช้ ถ้าหลุดหรือถูกเรียกมากเกินไปจากฝั่ง client ผู้ใช้ต้องรับผิดชอบค่าใช้จ่ายเอง
- [ ] **ระบบจริงต้องมีตัวกลาง (backend/Cloud Function) เก็บคีย์ไว้ฝั่งเซิร์ฟเวอร์ที่ผู้ใช้แตะไม่ถึง** ตอนนี้ (`test-ai.html` และ US-09 ตามที่ spec ออกแบบไว้) ยังเรียก OpenRouter ตรงจากเบราว์เซอร์ ซึ่งใครก็เปิด DevTools ดูคีย์ได้ — เป็นข้อจำกัดของ Module 2 เพราะ spec §0.2 ห้ามมี custom backend การเก็บคีย์ไว้ในไฟล์ที่ `.gitignore` กันไว้ (`js/config.js`) ป้องกันแค่ "ไม่หลุดขึ้น GitHub" ไม่ได้ป้องกัน "ผู้ใช้เห็นคีย์ในเบราว์เซอร์" — การมีตัวกลางจริงคืองาน Sprint 2
- [ ] **ก่อน push ทุกครั้ง ต้องตรวจว่าไม่มีไฟล์คีย์หลุดไปด้วย** รัน `git status` ให้แน่ใจว่าไม่มี `js/config.js` / `secrets.js` / `.env*` / ไฟล์ serviceAccount ติดอยู่ในรายการที่จะ commit ก่อนกด push ทุกครั้ง (ดูขั้นตอนตรวจสอบท้าย `.gitignore`)
