# ACL.md — ตารางสิทธิ์ผู้ใช้ระบบ LeaveEasy

> อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท), หัวข้อ 3 (User Story US-04, US-06, US-08) และหัวข้อ 6 (การเปลี่ยนสถานะ)
> ค่าที่เก็บในช่อง `role` บน Firestore ใช้รหัสภาษาอังกฤษเสมอ: `employee` · `manager` · `hr`

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) รวมถึง **อนุมัติใบลาของตัวเองไม่ได้** · เปิดดูใบลาของผู้ขอลาคนอื่น · แก้ไขประเภทการลา · ลบใบลาที่สถานะไม่ใช่ `รอพิจารณา` |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลาทุกใบของทุกคน · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไขประเภทการลา · เปลี่ยนสถานะย้อนกลับ (ใบที่อนุมัติ/ไม่อนุมัติแล้วแก้สถานะต่อไม่ได้อีก) · **อนุมัติใบลาของตัวเองไม่ได้**¹ |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ (ดูทุกใบ · เปลี่ยนสถานะ · เขียนความเห็น) · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบ ประเภทการลา · ดูแดชบอร์ดสรุปทั้งระบบ | **อนุมัติใบลาของตัวเองไม่ได้**¹ |

¹ **ข้อเสนอเพิ่มเติม ไม่ได้มาจาก spec ต้นฉบับ** — `leaveeasy-spec.md` หัวข้อ 2 ไม่ได้ระบุข้อห้ามนี้ไว้ (ช่อง "ทำอะไรไม่ได้" ของทั้ง manager และ hr เป็น "—" ในต้นฉบับ) แต่เพื่อป้องกันผลประโยชน์ทับซ้อน (ไม่ควรมีใครอนุมัติใบลาของตัวเองได้ ไม่ว่าจะมีสิทธิ์เปลี่ยนสถานะแค่ไหน) จึงเสนอให้เพิ่มกฎนี้ — ถ้าจะนำไปใช้จริงต้องเขียนเพิ่มใน Security Rules ตอนทำงานสัปดาห์ที่ 8 (เช็คว่า `request.auth.uid != resource.data.requesterId` ก่อนอนุญาตให้แก้ `status`)

## กฎร่วมทุกบทบาท

- **ต้องเข้าสู่ระบบก่อนเสมอ** — คนที่ไม่ได้ล็อกอินอ่าน/เขียนข้อมูลไม่ได้เลย
- สถานะใบลาเปลี่ยนได้ทางเดียวเท่านั้น: `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` — ถึงจะเป็นผู้อนุมัติหรือฝ่ายบุคคลก็เปลี่ยนย้อนกลับไม่ได้
- การเปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นในโฟลเดอร์ย่อย `approvals` อย่างน้อย 1 รายการก่อนเสมอ

## สถานะการบังคับใช้จริงในระบบตอนนี้

⚠️ ตารางนี้คือสิทธิ์ตามที่ spec ออกแบบไว้ **แต่ Firestore Security Rules ปัจจุบันยังบังคับแค่ "ต้องล็อกอินก่อน" เท่านั้น** (`allow read, write: if request.auth != null`) — ทุกคนที่ล็อกอินแล้วยังอ่าน/เขียนข้ามบทบาทกันได้อยู่ การแยกสิทธิ์ตามตารางนี้จริงจังเป็นงานสัปดาห์ที่ 8 (เขียน Security Rules ตรวจ `role` และเจ้าของใบลา)
